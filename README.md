# aws-serverless-support-analytics
End to end serverless ETL pipeline on AWS for simulated customer support call data. Processes raw JSON with Glue and stores cleaned results in S3, with a Streamlit dashboard that reads the curated dataset through
