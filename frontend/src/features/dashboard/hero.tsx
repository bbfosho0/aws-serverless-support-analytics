export function DashboardHero() {
  return (
    <section className="rounded-lg border p-6">
      <h4 className="text-lg font-semibold">Dashboard Hero</h4>
      <p className="text-muted-foreground">
        This module will orchestrate KPI cards, filters, and callouts once the data layer is wired.
      </p>
    </section>
  );
}
